RECOMP COACH — HANDOFF RECAP

Context:
Fresh-chat continuation for personal-use MVP (Path A). Goal is NOT multi-user or full generator yet. Goal is to make current program usable by manually cleaning program rows in Supabase, while keeping the app stable.

What got done today:
1. Clarified project status:
   - Chose Path A = manually clean current session rows in Supabase first.
   - Decided not to build the full generator yet.

2. Built metadata layer:
   - Created `src/data/exerciseMetadata.ts`
   - Added exercise metadata types and first-pass metadata entries for current split
   - Added `getExerciseMetadataByName(...)`

3. Built ordering engine:
   - Created `src/utils/orderSessionExercises.ts`
   - Added session-specific priority sorting for:
     - upperA
     - lowerA
     - upperB
     - lowerB
   - Added canonical override logic and normalization helpers
   - Ordering test passed after fixes

4. Wiring / TypeScript issues fixed:
   - Fixed wrong folder creation issue (`src\utils`, `src\data` vs real `src/utils`, `src/data`)
   - Restored deleted `exerciseMetadata.ts`
   - Restarted TS server
   - Cleared module resolution/editor errors
   - Confirmed localhost works again

5. Patched `src/App.tsx`:
   - Imported `orderSessionExercises`
   - Added session sorting logic for grouped program rows
   - Program tab now uses sorted session rows before render/selection

Important conclusion from testing:
- The sorter is working, but it only reorders exercises already present in a session.
- It does NOT fix bad session content.
- Therefore the next priority is manual cleanup of `program_templates` rows in Supabase.

Supabase work completed:
Table:
- `program_templates`

Filtered session worked on:
- `week_no = 1`
- `day_no = 1`
- `session_name = Upper A`

Upper A content was manually cleaned.

Current intended usable Upper A:
1. Chest-Supported Row
2. Floor Dumbbell Press
3. Neutral Grip Lat Pulldown
4. Tonal Fly
5. Lateral Raise
6. Cable Pushdown
7. Hammer Curl
8. Face Pull

Important naming lesson:
For the sorter/metadata to work correctly, exercise names in Supabase should match or closely map to metadata names. Good standardized names used/targeted:
- `Chest-Supported Row`
- `Floor Dumbbell Press`
- `Neutral Grip Lat Pulldown`
- `Tonal Fly`
- `Lateral Raise`
- `Cable Pushdown`
- `Hammer Curl`
- `Face Pull`

Supabase insert issue discovered:
- `program_templates.id` does NOT auto-generate cleanly from blank input in the Table Editor flow being used
- Copying an existing row ID causes duplicate key error
- Blank string for UUID causes invalid UUID error
- Workaround used: when inserting a new row manually, provide a real unique UUID
- Example UUID used once already for Face Pull row:
  - `11111111-1111-4111-8111-111111111111`
- DO NOT reuse that UUID again

Important product/engineering decisions:
- Stay on Path A for now
- Do NOT build generator yet
- Do NOT refactor working code unless necessary
- Use Supabase manual row cleanup to make Week 1 sessions usable
- Later, the cleaned sessions can become the reference model for a generator

Current app/code status:
- `exerciseMetadata.ts` compiles
- `orderSessionExercises.ts` works
- App loads
- Program tab sorting is wired in
- Program content quality still depends on actual Supabase row content

What should happen next session:
1. Continue manual cleanup in Supabase `program_templates`
2. Next target:
   - `week_no = 1`
   - `day_no = 2`
   - `session_name = Lower A`
3. Clean Lower A content to match intended template

Target Lower A:
- Core block first (stored as separate rows is acceptable for now)
- Goblet Squat
- Romanian Deadlift
- Leg Curl
- Leg Extension
- Calf Raises

For Lower A, storing core as separate rows is acceptable right now, e.g.:
- Bird Dog
- Pallof Press
- Side Plank
- McGill Curl Up

Then:
- remove rows that do not belong in Lower A
- standardize names so sorter/metadata recognize them

After Lower A:
- clean Upper B
- clean Lower B

Key rule for future assistant:
Do not give vague directions.
Use explicit location language only:
- In VS Code
- In the terminal
- In the browser
- In Supabase
- In SQL Editor
- In Table Editor

Also:
- inspect before changing
- minimal targeted edits only
- preserve working code

If resuming tomorrow:
Start by helping clean `program_templates` for:
- Week 1
- Day 2
- Lower A

And do it one session at a time.